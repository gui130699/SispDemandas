import type { Timestamp } from 'firebase/firestore'
export type Role = 'admin'|'consultant'|'requester'; export type Status = 'new'|'triage'|'waiting_information'|'analysis'|'development'|'testing'|'waiting_validation'|'completed'|'cancelled'; export type Priority = 'low'|'normal'|'high'|'urgent'
export interface UserProfile { uid:string; name:string; email:string; emailNormalized:string; role:Role; companyId:string|null; companyName?:string; active:boolean; createdAt?:Timestamp; updatedAt?:Timestamp }
export interface Company { id:string; legalName:string; legalNameNormalized:string; tradeName?:string; cnpj?:string; phone?:string; email?:string; contactName?:string; notes?:string; active:boolean }
export interface Demand { id:string; code:string; sequence:number; year:number; title:string; description:string; companyId:string; companyName:string; requesterId:string; requesterName:string; screenName:string; screenNameNormalized:string; levelId:string; levelName:string; formName:string; formNameNormalized:string; priority:Priority; status:Status; consultantId:string|null; consultantName:string|null; createdAt?:Timestamp; updatedAt?:Timestamp; lastActivityAt?:Timestamp; startedAt?:Timestamp|null; completedAt?:Timestamp|null; cancelledAt?:Timestamp|null }
export const statusLabels:Record<Status,string>={new:'Nova',triage:'Em triagem',waiting_information:'Aguardando informações',analysis:'Em análise',development:'Em desenvolvimento',testing:'Em teste',waiting_validation:'Aguardando validação',completed:'Concluída',cancelled:'Cancelada'}
